# Local Windows profile

The converter is a personal, single-user Windows 10/11 tool running on Java 17.
Inputs are local files selected by the user; there is no public Java API or
server compatibility promise. Fidelity of conversion is preferred over broad
cross-platform and hostile-input matrices, which remain deferred to later
tasks.
